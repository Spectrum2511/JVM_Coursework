import GUI_elements.*;
import classes.Project;
import classes.Team;
import classes.Task;
import javax.swing.JFrame;

public class TestingClass {

    public static void main(String[] args){
/*
        Project proj1 = new Project("Project JVM", "The JVM Coursework", "2021-11-05",100);
        proj1.setProjName("Hello World");

        proj1.createTeam("Team A", "Team for coding", 01);
        proj1.createTeam("Team B", "Team for debugging", 02);

        proj1.addTask("Create GUI", 14);
        proj1.AssignTeamToTask(proj1.getTeamOfName("Team A"), proj1.getTaskOfDescription("Create GUI"));

        proj1.addTask("Develop testbed", 20);
        proj1.AssignTeamToTask(proj1.getTeamOfName("Team B"), proj1.getTaskOfDescription("Develop testbed"));

        proj1.addTask("This should appear", 100);
        //proj1.AssignTeamToTask(proj1.getTeamOfName("Team B"));

        proj1.addTask("Grand Finale", 1);

        proj1.getProjectTasks().get(0).setTaskComplete();
        proj1.getProjectTasks().get(1).setTaskComplete();
        proj1.getProjectTasks().get(2).setTaskComplete();

        proj1.findCurrentTask();
        System.out.println(proj1.getCurrentTask().getTaskDesc());
        proj1.displayTaskData();
        System.out.println(proj1.getProjName());
        //warnWindow warning = new warnWindow("Warning: this is a warning");

        //JFrame j = new JVM();

*/
        JVM GUI = new JVM();
        //frame.setSize(700, 600);
    }
}
